<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
        <link rel="icon" type="image/x-icon" href="/images/favicon.ico" />
        <link rel="stylesheet" href="/stylesheets/style.css" />
    </head>

    <body>
        <%- include('nav_bar') %>
        <div class="content">
            <h1><%= title %>: <%= director.name %></h1>
            <p><%= director.date_of_birth_formatted %> - <%= director.date_of_death_formatted %></p>
            <% if (director_movies.length) { %>
            <p>
                <strong
                    >Delete the following movies before attempting to delete this director.</strong
                >
            </p>
            <div>
                <h4>Movies</h4>
                <dl>
                    <% director_movies.forEach((movie) => { %>
                    <dt>
                        <a href="<%= movie.url %>"><%= movie.title %></a>
                    </dt>
                    <dd><%= movie.summary %></dd>
                    <% }); %>
                </dl>
            </div>
            <% } else { %>
            <p>Do you really want to delete this Director?</p>
            <form method="POST" action="">
                <div class="form-group">
                    <input
                        id="directorid"
                        class="form-control"
                        type="hidden"
                        name="directorid"
                        required="true"
                        value="<%= director._id %>"
                    />
                </div>
                <button class="btn-primary" type="submit">Delete</button>
            </form>
            <% } %>
        </div>

        <footer>
            <p>Copyright © Laszlo Kis 2023</p>
            <a class="reflink" href="https://github.com/ev0clu" target="_blank">
                <img class="github-img" src="/images/github-logo.png" alt="Github logo" />
            </a>
        </footer>
    </body>
</html>
