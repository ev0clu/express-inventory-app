<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
        <link rel="icon" type="image/x-icon" href="/images/favicon.ico" />
        <link rel="stylesheet" href="/stylesheets/style.css" />
    </head>

    <body>
        <%- include('nav_bar') %>
        <div class="content">
            <h1>Genre: <%= genre.name %></h1>
            <div class="content-list">
                <% if (genre_movies.length > 0) { %> <% genre_movies.map(movie => { %>
                <dt>
                    <strong><a href="<%= movie.url %>"> <%= movie.title %></a> </strong>
                </dt>
                <dd><%= movie.summary %></dd>
                <% }) %> <% } else { %>
                <p>This genre has no movies in the database.</p>
                <% } %>
                <hr />
                <p>
                    <a href="<%= genre.url %>/delete">Delete director</a>
                </p>
            </div>
        </div>
        <footer>
            <p>Copyright © Laszlo Kis 2023</p>
            <a class="reflink" href="https://github.com/ev0clu" target="_blank">
                <img class="github-img" src="/images/github-logo.png" alt="Github logo" />
            </a>
        </footer>
    </body>
</html>
